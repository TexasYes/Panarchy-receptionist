# Riley LiveKit agent — Python dependencies
# Install:  pip install -r requirements.txt

# LiveKit Agents framework + plugins
livekit-agents>=0.12.0
livekit-plugins-anthropic>=0.4.0
livekit-plugins-openai>=0.10.0      # used for both OpenAI direct AND Grok (OpenAI-compatible API)
livekit-plugins-deepgram>=0.7.0
livekit-plugins-elevenlabs>=0.8.0
livekit-plugins-silero>=0.7.0

# HTTP client for our Railway tool callbacks
httpx>=0.27.0

# .env loader so the agent can read the same .env the Node scripts use
python-dotenv>=1.0.0
